# 1. Stage all changes
git add .

# 2. Commit the staged changes with a message
git commit -m "video UI controls implemented to one level for TIP template"

# 3. Push the new commit to the remote GitHub repository
git push
