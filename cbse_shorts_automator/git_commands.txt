# 1. Stage all changes
git add .

# 2. Commit the staged changes with a message
git commit -m "included logics for auto uploading to google drive after successful generation of each video"

# 3. Push the new commit to the remote GitHub repository
git push
