# 1. Stage all changes
git add .

# 2. Commit the staged changes with a message
git commit -m "Most of the bugs are fixed in Fact template. one level of stable code implemented for TIP template"

# 3. Push the new commit to the remote GitHub repository
git push
