# 1. Stage all changes
git add .

# 2. Commit the staged changes with a message
git commit -m "Implemented few mathematical optimisations for rendering. initial blank frames in rendering still persists"

# 3. Push the new commit to the remote GitHub repository
git push
