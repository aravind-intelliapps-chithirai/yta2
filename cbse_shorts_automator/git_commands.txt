# 1. Stage all changes
git add .

# 2. Commit the staged changes with a message
git commit -m "included the vid_out_filename.txt file for checking Colab GPU workflow"

# 3. Push the new commit to the remote GitHub repository
git push