# 1. Stage all changes
git add .

# 2. Commit the staged changes with a message
git commit -m "corrected emoji rendering and watermark issues in headless rendering"

# 3. Push the new commit to the remote GitHub repository
git push
